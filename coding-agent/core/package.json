{
  "name": "@veltravia/coding-agent-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Veltravia AI Coding Agent Core: a controlled software-engineering agent - explicit state machine, validated plans and actions, every operation through the Tool System (Project Engine file tools + sandbox validation), bounded iteration, human approval, cancellation, audit. Provider-neutral; never touches host filesystem, shells, credentials, or provider SDKs.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@veltravia/project-core": "*",
    "@veltravia/tool-core": "*"
  }
}
