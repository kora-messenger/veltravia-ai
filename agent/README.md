# agent/ — the AI agent layer

The **agent orchestration layer**: provider-neutral agents that reason about a task, request tools through the Tool System, process normalized results, and complete within hard limits. This is an ORCHESTRATION layer — not a coding agent, not a project engine, not self-development.

```
agent/
├── core/   # @veltravia/agent-core - IMPLEMENTED (Step 6)
└── mock/   # @veltravia/agent-mock - IMPLEMENTED (Step 6)
```

Details: [docs/agents.md](../docs/agents.md).
