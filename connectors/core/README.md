# connectors/core/

The connector system's foundation: a shared connector interface, lifecycle management (connect/refresh/revoke), and credential isolation. Connector credentials live only here — never in generated projects, never in AI prompts.

**Intentionally empty in Step 1.**
