# project-engine/filesystem/

Virtual filesystem abstraction over generated projects: path-traversal guards, allow-listed roots, snapshot-friendly reads/writes. The only layer allowed to touch project files.

**Intentionally empty in Step 1.**
