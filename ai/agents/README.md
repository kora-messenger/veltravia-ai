# ai/agents/

Higher-level agent definitions (planner, builder, reviewer, debugger) that compose prompts and tool calls into complete workflows, on top of `ai/core`.

**Intentionally empty in Step 1.** Becomes workspaces (`@veltravia/ai-agent-*`) when agent workflows are designed.
